//window -> PERTENECE AL BOM (BROWSER OBJECT MODEL)
//document -> PERTENECE AL DOM (DOCUMENT OBJECT MODEL)

const $titulo = document.getElementById("titulo"); //CASE SENSITIVE

$titulo.style.fontSize = "10rem";
